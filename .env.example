# Resend (https://resend.com) — used by /api/quote to email quote form submissions
RESEND_API_KEY=
# Where quote request emails should be delivered
QUOTE_TO_EMAIL=
# Optional: verified sending address once a domain is set up in Resend.
# Defaults to onboarding@resend.dev (Resend's shared testing address) if unset.
QUOTE_FROM_EMAIL=
